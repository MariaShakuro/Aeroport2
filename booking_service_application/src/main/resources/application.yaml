spring:
  application:
    name: core

  datasource:
     url: jdbc:mysql://mysql:3306/tickets_for_aeroport?createDatabaseIfNotExist=true
     username: root
     password: dsova2207_12
     driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
   hibernate:
    ddl-auto: update
   show-sql: true
   properties:
    hibernate:
     dialect: org.hibernate.dialect.MySQL8Dialect

  liquibase:
   enabled: false

server:
  post: 8084